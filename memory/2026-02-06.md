# 2026-02-06

## 主要事项

- 查询天气：使用自定义 skill **gaode-weather**（高德天气 API，city=110114 昌平）。
- 讨论并确认：不直接删除系统自带 `weather` skill，倾向“禁用/屏蔽”。（当时发现缺少明确的本地 skills 开关入口，后续需要通过 Gateway 配置/allowlist/denylist 方式处理。）
- 尝试做 skills 可用性/心跳检查时，发现：
  - `openclaw gog ...` 在该环境下并不是有效的 CLI 子命令（unknown command 'gog'）。
  - HEARTBEAT.md 期望检查 Gmail/日历，但当时没有可用的 gog 调用路径来完成。

## 模型切换

- 将默认模型从 `zenmux/openai/gpt-5.2` 切换到 `zenmux/moonshotai/kimi-k2.5`，并保留 `zenmux/openai/gpt-5.2` 作为 fallback。
- 触发 Gateway 重启，随后确认 `openclaw models list` 中 kimi-k2.5 成为 default。

## 备注/问题

- 记忆搜索（embeddings）后续出现 401 invalid_api_key 问题，导致 memory_search 无法使用（需要修复 embeddings 的 OpenAI key/配置）。
