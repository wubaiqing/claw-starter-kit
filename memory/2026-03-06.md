# 2026-03-06

## 工作 / 项目
- 修复并调整 OpenClaw 的「记忆检索 / memory_search」配置：从 OpenAI embeddings 报 401 的状态，改为走火山方舟（Ark）OpenAI 兼容 embeddings。
- 为避免 Key 落盘：新增 secrets env provider（allowlist `HUOSHAN_KEY`），让 memorySearch 从环境变量读取 API Key。
- 将 memorySearch 的 embeddings 模型从模型名尝试（`doubao-embedding-large-text-250515`）切换为火山方舟推理接入点 `ep-20241203112812-kqrjt`（因模型名无权限/不存在、随后检索超时，改走 endpoint）。

## 学习 / 成长
- 理清：openai-codex OAuth 仅覆盖对话链路；memory_search 需要单独可用的 embeddings provider。

## 记录 / 备忘
- 记忆检索验证口令："从长期记忆里检索：我对 TTS 的偏好是什么？"
- 已知记忆：TTS 偏好为优先阿里云 CosyVoice（记在 2026-02-11）。
