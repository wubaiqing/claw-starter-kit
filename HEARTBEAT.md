# HEARTBEAT.md - 心跳检查项

## 定期任务

记忆整理(每次心跳检查)

- 检查今天的 `memory/YYY-MM-DD.md` 是否存在如果本次会话有未记录的重要操作，补写日记
- 检查 `MEMORY.md` 是否需要更新(新的架构决策、踩坑记录)

## 记忆卫生(每周一次，周一心跳时执行)

- 检查 `workspace` 根目录是否有不该在的大文件(>5KB的非标准.md)
- 检查 `memory/` 下 `>60` 天的日记，提醒归档
- 不要自动删除，只提醒
